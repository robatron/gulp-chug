#! /bin/bash

# Update published 0.1.x branch, i.e.,
#   1. Move 0.1 tag -> HEAD, push
#   2. Publish -> NPM registry

TAG_NAME="0.1"
TAG_MSG="IPO"

git push origin-http master
git tag -a $TAG_NAME -m "$TAG_MSG" --force
git push origin-http $TAG_NAME --force
npm publish
